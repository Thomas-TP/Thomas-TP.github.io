# Cloudflare WAF Configuration
# This file should be used if you migrate to Cloudflare

# ===== WAF RULES =====
# Block common attack patterns
(cf.WAF) Block SQL injection attempts
(cf.WAF) Block XSS attempts
(cf.WAF) Block command injection
(cf.WAF) Block path traversal

# ===== RATE LIMITING =====
# Rate limit API calls
(cf.rate_limit) /api/*: 100 requests per 10 minutes

# ===== SECURITY HEADERS =====
# Additional security headers (complementing meta tags)
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=(), interest-cohort=()

# ===== CSP HEADERS (if not using meta tags) =====
# Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com https://cdnjs.cloudflare.com https://smtpjs.com https://cdn.botpress.cloud https://files.bpcontent.cloud https://webchat.botpress.cloud; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com https://cdnjs.cloudflare.com https://files.bpcontent.cloud; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: https: blob:; connect-src 'self' https://formspree.io https://cdn.botpress.cloud https://webchat.botpress.cloud https://streak-stats.demolab.com https://git.io; frame-src 'none'; object-src 'none'; base-uri 'self'; form-action 'self' https://formspree.io; upgrade-insecure-requests;

# ===== BOT PROTECTION =====
# Block known bad bots
(cf.bot_management) Block bad bots

# ===== DDoS PROTECTION =====
# Enable DDoS protection
(cf.ddos_protection) Enable

# ===== SSL/TLS =====
# Force HTTPS
(cf.ssl) Full (strict)

# ===== PAGE RULES =====
# Redirect HTTP to HTTPS
http://thomas-tp.github.io/* -> https://thomas-tp.github.io/:301

# ===== FIREWALL RULES =====
# Block countries with high attack rates (optional)
# (cf.country) Block: RU, CN, IN

# ===== CUSTOM RULES =====
# Block access to sensitive files
/*/.env -> 403
/*/wp-admin/* -> 403
/*/admin/* -> 403
/*/.git/* -> 403
/*/phpmyadmin/* -> 403

# ===== MONITORING =====
# Log security events
(cf.logging) Enable security logs